{
  "name": "express-default",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "cd back && npm start",
    "heroku-postbuild": "cd front && npm install && npm run build && cp -r build ../back/"
  }
}